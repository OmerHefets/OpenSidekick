/**
 * Enum for storage types available in Chrome extension
 * @enum {string}
 */
export const StorageEnum = {
    Local: "local",
    Session: "session",
    Sync: "sync",
};

/**
 * Enum for storage categories to organize data
 * @enum {string}
 */
export const StorageCategoryEnum = {
    ApiKeys: "api-keys",
    Preferences: "preferences",
    UserData: "user-data",
};
